// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
	"name": "Python 3",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/python:1-3.12",

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {
			"settings": {},
			"extensions": [
				"streetsidesoftware.code-spell-checker"
			]
		}
	},
	
	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [9000],

	// Use 'portsAttributes' to set default properties for specific forwarded ports. 
	// More info: https://containers.dev/implementors/json_reference/#port-attributes
	"portsAttributes": {
		"9000": {
			"label": "Hello Remote World",
			"onAutoForward": "notify"
		}
	},

	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": "pip3 install -r requirements.txt"

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
{
         }

$*!* build preboot :toolbox;restore allof allin alltools inclusive for user(0); then establish parm demensions (model- CPH2515 / Processor - Qualcomm Snapdragon 695 5G Octa-core/ Battery Capacity 5000 mAH (TYP)/ Screen Size 6.72 in/ RAM>8GB*32GB if this is true *!*
then $ is true 
su then $*!*; store in local "*Purging"; then zipper 
Check userid for buggs ; only this device    
handler Listen for callback ;recived true
$*!*; if unzipper must use deviceid user fingerprint 'true' then if "false" destroy "*Purging " when denied "true" null
then
$;*!* Job Todo: RESTORE 5G TO this  device;"true" block noncarrier/unathuthorized handler and vendors  ITokenmaster  doesnt aprove!"true"
then
Super Job completed!--------------"true"
$*!*; 
    then                  *Note: pgk install preboot 
pre-built accessibility Infinity Tokenmaster Heavy-duty BEASTMODE Busybox.tar.2.0  in /system/bin/broot_root_chroot only Good



 accessibility Infinity Tokenmaster can aprove being authorized  "MY" vendor-oplus latest os with extra-security pentested and  fire proof to outsider handler ! listen for halfback quaterback runningback callback drawback fallback true recived as admin throw all null nodes back ; fetch and catch all verified host watch for imposters among usid . VPN MASK ON AT ALL TIMES İN CONJUNCTION WITH/WITHOUT VENDOR SECURITY! true 
outside handler can recive error notification true 
; then block outside handler fetch device data fetch app permissions that is draining battary consuptıon ; then configure and fix it then
q
   
!!!!!!$*!* build infinity Token "true" this will stay hidden in lovebox  __  parm dementions shall not exceed those of  pre built dimensions in' $line_4' "true"

 !*!* $line16 is fireproof "true if $line4 is true" if then "false"execute (grey*Removing )    {  {   {


}}}}}}}} Job Todo Finnished *!*  !{{{{{{{{

*echo this curl * DONE *Purging!
